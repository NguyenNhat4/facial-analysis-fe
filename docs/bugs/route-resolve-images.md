Because in Vite, files in the public folder are served from the site root. When you use a relative path like assets/... on a nested route (e.g., /patients/123/upload), the browser resolves it as /patients/123/assets/... and the file 404s. Using /assets/... makes it absolute from the root, so it always resolves to public/assets/....